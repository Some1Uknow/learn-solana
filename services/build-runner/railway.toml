[build]
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "/usr/local/bin/learnsol-build-runner"
healthcheckPath = "/healthz"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
