---
title: Challenge 11 - Post Only Rejection
description: >-
  Reject post-only orders that would immediately cross the top of book and take liquidity.
keywords:
  - post only order
  - maker order
  - orderbook crossing
track: orderbook
slug: post-only-rejection
order: 11
difficulty: Medium
tags:
  - Orderbook
  - Post Only
  - Maker Taker
starterCode: |-
  enum Side {
      Bid,
      Ask,
  }

  fn accepts_post_only(side: Side, price: u64, best_bid: Option<u64>, best_ask: Option<u64>) -> bool {
      // A post-only bid is rejected if price >= best_ask.
      // A post-only ask is rejected if price <= best_bid.
      true
  }

  fn main() {
      let bid_ok = accepts_post_only(Side::Bid, 99, Some(98), Some(100));
      let bid_rejected = accepts_post_only(Side::Bid, 100, Some(98), Some(100));
      println!("{} {}", bid_ok, bid_rejected);
  }
executor:
  type: rust-playground
  description: Reject post-only orders that would cross immediately.
  channel: stable
  edition: '2021'
  mode: debug
  tests:
    - name: main
      expectedStdout: 'true false'
---

# Challenge 11: Post Only Rejection

Post-only orders are intended to add maker liquidity, not immediately take liquidity.

## Task

Implement `accepts_post_only`.

## Rules

- Post-only bid is rejected when it crosses the best ask.
- Post-only ask is rejected when it crosses the best bid.
- If the opposite side is empty, the order can post.

This introduces maker/taker behavior without adding fee complexity.
