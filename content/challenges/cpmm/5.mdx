---
title: Challenge 5 - Slippage Protected Swap
description: >-
  Combine fee-adjusted input, constant-product quote math, and a minimum output
  check to protect the trader from excessive slippage.
keywords:
  - cpmm slippage
  - min out
  - amm protection
track: cpmm
slug: slippage-protected-swap
order: 5
difficulty: Medium
tags:
  - CPMM
  - Slippage
  - Swaps
starterCode: |-
  fn protected_swap_output(x_reserve: u64, y_reserve: u64, x_in: u64, min_out: u64) -> Result<u64, &'static str> {
      // 1. Apply a 30 bps fee to x_in.
      // 2. Quote the output with constant-product math.
      // 3. Return Err("slippage exceeded") if output < min_out.
      Err("not implemented")
  }

  fn main() {
      match protected_swap_output(1_000, 2_000, 100, 180) {
          Ok(amount) => println!("{}", amount),
          Err(message) => println!("{}", message),
      }
  }
executor:
  type: rust-playground
  description: Quote a fee-adjusted swap and enforce a minimum output.
  channel: stable
  edition: '2021'
  mode: debug
  tests:
    - name: main
      expectedStdout: '181'
---

# Challenge 5: Slippage Protected Swap

This is the first mini end-to-end CPMM exercise.

## Task

Implement `protected_swap_output`.

## Flow

1. Apply the 30 bps fee.
2. Use the exact-in constant-product quote.
3. Compare the result to `min_out`.
4. If the quote is too small, reject the trade.

## Sample Walkthrough

With:

```txt
x = 1000
y = 2000
x_in = 100
min_out = 180
```

You should:

- fee-adjust `100` to `99`
- compute output using `new_x = 1099`
- get `181`
- accept because `181 >= 180`

## Important

Round down during division. Conservative rounding is safer for AMM math.
