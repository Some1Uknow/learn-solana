---
title: Challenge 20 - Secure CPMM Swap Flow
description: >-
  Combine pause-state validation, fee-adjusted exact-in quoting, and slippage
  protection into one secure swap function.
keywords:
  - cpmm secure swap
  - amm end to end
  - slippage protected flow
track: cpmm
slug: secure-cpmm-swap-flow
order: 20
difficulty: Hard
tags:
  - CPMM
  - End to End
  - Security
starterCode: |-
  enum PoolState {
      Active,
      Paused,
  }

  fn secure_swap_output(state: PoolState, x_reserve: u64, y_reserve: u64, x_in: u64, min_out: u64) -> Result<u64, &'static str> {
      // 1. Reject if the pool is paused with Err("pool paused").
      // 2. Apply a 30 bps fee to x_in.
      // 3. Compute the constant-product output.
      // 4. Reject with Err("slippage exceeded") if output < min_out.
      // 5. Otherwise return Ok(output).
      Err("not implemented")
  }

  fn main() {
      match secure_swap_output(PoolState::Active, 1_000, 2_000, 100, 180) {
          Ok(value) => println!("{}", value),
          Err(message) => println!("{}", message),
      }
  }
executor:
  type: rust-playground
  description: Execute a secure fee-adjusted swap with state and slippage checks.
  channel: stable
  edition: '2021'
  mode: debug
  tests:
    - name: main
      expectedStdout: '181'
---

# Challenge 20: Secure CPMM Swap Flow

This challenge combines the most important ideas in the track.

## Task

Implement `secure_swap_output`.

## Required behavior

### 1. State guard

If the pool is paused, return:

```txt
Err("pool paused")
```

### 2. Fee-adjusted input

Use:

```txt
effective_input = x_in * 9_970 / 10_000
```

### 3. Constant-product quote

Use:

```txt
k = x_reserve * y_reserve
new_x = x_reserve + effective_input
new_y = k / new_x
output = y_reserve - new_y
```

### 4. Slippage guard

If `output < min_out`, return:

```txt
Err("slippage exceeded")
```

### 5. Success case

Return:

```txt
Ok(output)
```

## Sample

For `(Active, 1000, 2000, 100, 180)`, the correct result is:

```txt
181
```
